<!DOCTYPE html>
<html lang="en">
  <%-include('./partials/head')%>
  <body>
    <main>
      <div class="container">
        <div class="text-center">
          <img src="/images/logo.png" alt="" class="user_logo" />
        </div>
        <div class="user_form border p-3">
          <h3 class="mb-3">Login Account</h3>
          <form action="/login" method="POST">
            <div class="form-group">
              <label for="username" class="form-label">Username</label>
              <input
                type="text"
                class="form-control"
                name="username"
                id="username"
                required
              />
            </div>
            <div class="form-group my-4">
              <label for="password" class="form-label">Password</label>
              <input
                type="password"
                class="form-control"
                name="password"
                id="password"
                required
              />
              <p class="text-danger"><%=message%></p>
            </div>
            <button type="submit" class="btn btn-primary">Login</button>
            <br />
            <div class="mt-3 px-auto d-flex">
              <p>Doesn't have an account?</p>
              <a href="/user/register" class="px-1">Register</a>
            </div>
          </form>
        </div>
        <section class="page_detail_links mt-2 text-center">
          <a href="" style="font-size: 14px; margin-right: 0.5rem">About Us</a>
          <a href="" style="font-size: 14px">Contacts</a>
        </section>
      </div>
    </main>
    <%-include('./partials/scripts')%>
  </body>
</html>
